pipeline {
    agent {
        docker {
            image 'eclipse-temurin:21-jdk'
        }
    }

    stages {
        stage('Test') {
            steps {
                sh 'java -version'
                sh 'javac -version'
            }
        }
    }
}